{# {{ node.literal|escape|unescape_single_quotes }} #}
{%- if configuration.get('html_input') == 'escape' -%}
{{ node.literal|escape|unescape_single_quotes }}
{%- elseif configuration.get('html_input') == 'strip' -%}
{{ node.literal|striptags }}
{%- else -%}
{{ node.literal }}
{%- endif -%}